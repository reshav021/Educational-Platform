import type { ReactNode } from 'react';
import type { LabelRenderProps } from './Label';
export declare type LabelRenderFunction<DataEntry extends BaseDataEntry> = (labelRenderProps: LabelRenderProps<DataEntry>) => ReactNode;
export declare type BaseDataEntry = {
    title?: string | number;
    color: string;
    value: number;
    key?: string | number;
};
declare type BaseExtendedDataEntry<DataEntry extends BaseDataEntry = BaseDataEntry> = DataEntry & {
    degrees: number;
    startAngle: number;
    percentage: number;
};
export declare type Data<DataEntry extends BaseDataEntry = BaseDataEntry> = DataEntry[];
export declare type ExtendedData<DataEntry extends BaseDataEntry = BaseDataEntry> = BaseExtendedDataEntry<DataEntry>[];
export {};
