import type { CSSProperties } from 'react';
import type { ExtendedData, BaseDataEntry, LabelRenderFunction } from './commonTypes';
export declare type LabelRenderProps<DataEntry extends BaseDataEntry> = {
    x: number;
    y: number;
    dx: number;
    dy: number;
    textAnchor: string;
    dataEntry: ExtendedData<DataEntry>[number];
    dataIndex: number;
    style?: CSSProperties;
};
export default function ReactMinimalPieChartLabel<DataEntry extends BaseDataEntry>({ renderLabel, labelProps, }: {
    renderLabel: LabelRenderFunction<DataEntry>;
    labelProps: LabelRenderProps<DataEntry>;
}): JSX.Element | null;
